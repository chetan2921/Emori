import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should launch', (WidgetTester tester) async {
    // Placeholder — real widget tests should mock providers and dotenv
  });
}
